import React from "react";

/**
 * This will be the Cover section of 
 * the portfolio.
 * @returns React.JSX
 */
export const Cover: React.FC = () => {
    return <div className="w-full h-screen bg-blue-500" id='cover'>Cover</div>;
};

Cover.displayName = 'Cover'